syntax = "proto2";

package navirice.proto;

message RequestHeader {
    enum Type {
        CURRENT_STEP = 0;
        CURRENT_LOCATION = 1;
    }
   required Type type = 1;
   required uint32 length = 2;
}
